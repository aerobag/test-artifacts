# METAR fixtures

`delta-three-hour/` contains a three-hour capture of generated METAR product
ZIPs. The source repo uses it to test live-feed METAR delta reconstruction and
to build local dev live-feed fixtures.
